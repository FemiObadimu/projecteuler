let palindrome = [];
function getPalindrome() {
  for (let index = 100; index < 1000; index++) {
    if (index * index >= 100000) {
      palindrome.push(index);
    }
  }
  console.log(a.element);

}

console.log(getPalindrome());
